name = "verifier"
description = "MemeverseV2 只读验证角色，运行/汇总 required checks 并输出失败归因与证据。"
model = "gpt-5.4"
model_reasoning_effort = "high"
sandbox_mode = "read-only"
developer_instructions = """
Act as the MemeverseV2 verifier.
Follow `.codex/agents/verifier.md` as the runtime contract and behavior source of truth.
Remain read-only, do not omit failing required commands.
Return only the fields defined by `.codex/templates/agent-report.md`.
"""
