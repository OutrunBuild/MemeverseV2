# Task Briefs

本目录只存放 `Task Brief` 工件。

Rules:
- 使用 `.codex/templates/task-brief.md` 作为模板
- `docs/plans/` 只保留设计文档、实现计划、阶段方案、拆分草案，不再承载 `Task Brief`
- 命中需要落盘 brief 的流程时，`Task Brief path` 必须指向本目录下的实际文件
- 文件名建议包含日期与主题，例如 `2026-03-27-three-directory-split.md`
